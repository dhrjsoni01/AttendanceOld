<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dks.attendance" >
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginpage"
            android:label="Login"></activity>
        <activity android:name=".About"></activity>
        <activity android:name=".registerpage"
            android:label="Register"></activity>
        <activity android:name=".Branch"
            android:label="Welcome"></activity>
        <activity android:name=".Record"
            android:label="Welcome"></activity>
        <activity android:name=".Subject"
            android:label="Select Subject"></activity>
        <activity android:name=".Record2"
            android:label="Record"></activity>
        <activity android:name=".Attendance2"></activity>
    </application>

</manifest>